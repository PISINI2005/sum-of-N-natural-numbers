#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter the number of natural numbers: ";
    cin >> n;
    int sum = (n * (n + 1)) / 2;
    cout << sum << endl;
    return 0;
}
